---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: instances.compute.v1.secapi.cloud
spec:
  group: compute.v1.secapi.cloud
  names:
    kind: Instance
    listKind: InstanceList
    plural: instances
    shortNames:
    - instance
    singular: instance
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Instance is the API for managing compute instances.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          commonData:
            description: CommonData defines the additional common fields that can
              be set on resources
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Annotations User-defined key/value pairs that are mutable and can be used to add annotations.
                  The number of annotations is eventually limited by the CSP.
                type: object
              extensions:
                additionalProperties:
                  type: string
                description: |-
                  Extensions User-defined key/value pairs that are mutable and can be used to add extensions.
                  Extensions are subject to validation by the CSP, and any value that is not accepted will be rejected during admission.
                type: object
              labels:
                description: |-
                  Labels User-defined key/value pairs that are mutable and can be used to
                  organize and categorize resources. We store the keys explicitly in the spec, because the values will be stored
                  directly in the Kubernetes labels.
                items:
                  type: string
                type: array
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceSpec Specification of the instance, including its
              SKU, network configuration, and storage options.
            properties:
              additionalNicRefs:
                description: AdditionalNicRefs Additional NICs attached to this instance
                items:
                  description: |-
                    Reference A reference to a resource using an object. The object contains the
                    same information as the ReferenceURN, but is represented as a structured object.
                    The advantage of this representation is that it can be used to reference
                    resources in different workspaces or regions without the need to specify
                    the full URN.
                  properties:
                    provider:
                      description: Provider Provider of the resource. If not set,
                        the provider is inferred from the context.
                      maxLength: 64
                      type: string
                    region:
                      description: Region Region of the resource. If not set, the
                        region is inferred from the context.
                      maxLength: 64
                      type: string
                    resource:
                      description: |-
                        Resource Resource-specific path identifying the resource within its workspace context.
                        This is the segment of the full URN after the provider, version, tenant, and
                        workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                        (nested) resources it includes the parent path segments:
                          `networks/<network-name>/route-tables/<rt-name>`
                        The provider, tenant, and workspace can be specified as separate fields in this
                        object; they do not need to be repeated here.
                      maxLength: 256
                      minLength: 1
                      type: string
                    tenant:
                      description: Tenant Tenant of the resource. If not set, the
                        tenant is inferred from the context.
                      maxLength: 64
                      type: string
                    workspace:
                      description: Workspace Workspace of the resource. If not set,
                        the workspace is inferred from the context.
                      maxLength: 64
                      type: string
                  required:
                  - resource
                  type: object
                maxItems: 16
                type: array
              antiAffinityGroup:
                description: |-
                  AntiAffinityGroup Anti-affinity group to which this instance belongs.
                  Instances in the same anti-affinity group are placed on different physical hosts.
                  The number of maximum instances in an anti-affinity group is provider-specific.
                maxLength: 64
                type: string
              bootVolume:
                description: BootVolume Reference to the block storage used to store
                  the boot volume of the instance.
                properties:
                  deviceRef:
                    description: DeviceRef Reference to the block storage used to
                      store the volume.
                    properties:
                      provider:
                        description: Provider Provider of the resource. If not set,
                          the provider is inferred from the context.
                        maxLength: 64
                        type: string
                      region:
                        description: Region Region of the resource. If not set, the
                          region is inferred from the context.
                        maxLength: 64
                        type: string
                      resource:
                        description: |-
                          Resource Resource-specific path identifying the resource within its workspace context.
                          This is the segment of the full URN after the provider, version, tenant, and
                          workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                          (nested) resources it includes the parent path segments:
                            `networks/<network-name>/route-tables/<rt-name>`
                          The provider, tenant, and workspace can be specified as separate fields in this
                          object; they do not need to be repeated here.
                        maxLength: 256
                        minLength: 1
                        type: string
                      tenant:
                        description: Tenant Tenant of the resource. If not set, the
                          tenant is inferred from the context.
                        maxLength: 64
                        type: string
                      workspace:
                        description: Workspace Workspace of the resource. If not set,
                          the workspace is inferred from the context.
                        maxLength: 64
                        type: string
                    required:
                    - resource
                    type: object
                  type:
                    default: virtio
                    description: Type The connection type depends on the type of device
                      and type of block storage.
                    enum:
                    - virtio
                    maxLength: 7
                    type: string
                required:
                - deviceRef
                type: object
              dataVolumes:
                items:
                  description: VolumeReference Represents a connection between a Block
                    Storage and an a user of the block storage.
                  properties:
                    deviceRef:
                      description: DeviceRef Reference to the block storage used to
                        store the volume.
                      properties:
                        provider:
                          description: Provider Provider of the resource. If not set,
                            the provider is inferred from the context.
                          maxLength: 64
                          type: string
                        region:
                          description: Region Region of the resource. If not set,
                            the region is inferred from the context.
                          maxLength: 64
                          type: string
                        resource:
                          description: |-
                            Resource Resource-specific path identifying the resource within its workspace context.
                            This is the segment of the full URN after the provider, version, tenant, and
                            workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                            (nested) resources it includes the parent path segments:
                              `networks/<network-name>/route-tables/<rt-name>`
                            The provider, tenant, and workspace can be specified as separate fields in this
                            object; they do not need to be repeated here.
                          maxLength: 256
                          minLength: 1
                          type: string
                        tenant:
                          description: Tenant Tenant of the resource. If not set,
                            the tenant is inferred from the context.
                          maxLength: 64
                          type: string
                        workspace:
                          description: Workspace Workspace of the resource. If not
                            set, the workspace is inferred from the context.
                          maxLength: 64
                          type: string
                      required:
                      - resource
                      type: object
                    type:
                      default: virtio
                      description: Type The connection type depends on the type of
                        device and type of block storage.
                      enum:
                      - virtio
                      maxLength: 7
                      type: string
                  required:
                  - deviceRef
                  type: object
                maxItems: 64
                type: array
              primaryNicRef:
                description: PrimaryNicRef Reference to the primary NIC attached to
                  this instance.
                properties:
                  provider:
                    description: Provider Provider of the resource. If not set, the
                      provider is inferred from the context.
                    maxLength: 64
                    type: string
                  region:
                    description: Region Region of the resource. If not set, the region
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  resource:
                    description: |-
                      Resource Resource-specific path identifying the resource within its workspace context.
                      This is the segment of the full URN after the provider, version, tenant, and
                      workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                      (nested) resources it includes the parent path segments:
                        `networks/<network-name>/route-tables/<rt-name>`
                      The provider, tenant, and workspace can be specified as separate fields in this
                      object; they do not need to be repeated here.
                    maxLength: 256
                    minLength: 1
                    type: string
                  tenant:
                    description: Tenant Tenant of the resource. If not set, the tenant
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  workspace:
                    description: Workspace Workspace of the resource. If not set,
                      the workspace is inferred from the context.
                    maxLength: 64
                    type: string
                required:
                - resource
                type: object
              securityGroupRef:
                description: SecurityGroupRef Reference to the security group associated
                  with this instance.
                properties:
                  provider:
                    description: Provider Provider of the resource. If not set, the
                      provider is inferred from the context.
                    maxLength: 64
                    type: string
                  region:
                    description: Region Region of the resource. If not set, the region
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  resource:
                    description: |-
                      Resource Resource-specific path identifying the resource within its workspace context.
                      This is the segment of the full URN after the provider, version, tenant, and
                      workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                      (nested) resources it includes the parent path segments:
                        `networks/<network-name>/route-tables/<rt-name>`
                      The provider, tenant, and workspace can be specified as separate fields in this
                      object; they do not need to be repeated here.
                    maxLength: 256
                    minLength: 1
                    type: string
                  tenant:
                    description: Tenant Tenant of the resource. If not set, the tenant
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  workspace:
                    description: Workspace Workspace of the resource. If not set,
                      the workspace is inferred from the context.
                    maxLength: 64
                    type: string
                required:
                - resource
                type: object
              skuRef:
                description: |-
                  SkuRef Reference to the SKU of the instance. The SKU is immutable after the instance is created.
                  To change the SKU, the instance must be deleted and recreated with the new SKU reference.
                properties:
                  provider:
                    description: Provider Provider of the resource. If not set, the
                      provider is inferred from the context.
                    maxLength: 64
                    type: string
                  region:
                    description: Region Region of the resource. If not set, the region
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  resource:
                    description: |-
                      Resource Resource-specific path identifying the resource within its workspace context.
                      This is the segment of the full URN after the provider, version, tenant, and
                      workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                      (nested) resources it includes the parent path segments:
                        `networks/<network-name>/route-tables/<rt-name>`
                      The provider, tenant, and workspace can be specified as separate fields in this
                      object; they do not need to be repeated here.
                    maxLength: 256
                    minLength: 1
                    type: string
                  tenant:
                    description: Tenant Tenant of the resource. If not set, the tenant
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  workspace:
                    description: Workspace Workspace of the resource. If not set,
                      the workspace is inferred from the context.
                    maxLength: 64
                    type: string
                required:
                - resource
                type: object
                x-kubernetes-validations:
                - message: spec.skuRef is immutable
                  rule: self == oldSelf
              sshKeys:
                description: |-
                  SshKeys Provider-specific references to SSH keys used in cloud-init vendorData.
                  These references are used to inject SSH public keys during instance initialization
                  through cloud-init's vendor data configuration.
                items:
                  maxLength: 4096
                  minLength: 1
                  type: string
                maxItems: 32
                type: array
              userData:
                description: |-
                  UserData Cloud-init user data for instance initialization
                  Example cloud-init user configuration with SSH key:
                maxLength: 65536
                type: string
              zone:
                description: |-
                  Zone The zone in which the instance is deployed. The zone is immutable after the instance is created.
                  To change the zone, the instance must be deleted and recreated with the new zone.
                maxLength: 32
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: spec.zone is immutable
                  rule: self == oldSelf
            required:
            - bootVolume
            - skuRef
            - zone
            type: object
          status:
            description: InstanceStatus defines model for InstanceStatus.
            properties:
              conditions:
                items:
                  description: |-
                    StatusCondition StatusCondition describes the state of a resource at a certain point.
                    Conditions are provider-specific and can represent different states depending on the
                    resource type and provider implementation.
                  properties:
                    lastTransitionAt:
                      description: |-
                        LastTransitionAt LastTransitionAt is the last time the condition transitioned from one
                        status to another. This should be when the underlying condition changed.
                        If that is not known, then using the time when the API field changed is
                        acceptable.
                      format: date-time
                      type: string
                    message:
                      description: Message A human-readable message indicating details
                        about the transition.
                      maxLength: 32768
                      type: string
                    occurrences:
                      type: integer
                    reason:
                      description: |-
                        Reason The reason for the condition's last transition in CamelCase.
                        The specific set of reason values is provider-specific and should be
                        documented by the provider.
                      maxLength: 1024
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    state:
                      description: |-
                        State Current phase of the resource:
                        - pending: not available, waiting for other resources
                        - creating: not available, creation started
                        - active: available for data layer usage
                        - updating: available for data layer usage
                        - deleting: maybe still available for data layer user, can fail any moment
                        - error: failed to fulfill the request; would be related to provider issue or customer related input.
                      type: string
                    type:
                      description: |-
                        Type Type of condition. The condition type is provider-specific and should
                        reflect the specific states relevant to your resource.
                      type: string
                  required:
                  - lastTransitionAt
                  - occurrences
                  - state
                  type: object
                maxItems: 32
                type: array
              powerState:
                description: PowerState Current power state of the instance.
                enum:
                - "on"
                - "off"
                type: string
              powerStateSince:
                description: PowerStateSince Indicates the time when the instance
                  powerState has updated, basecause the instance was started, stoped
                  or restarted. If the instance was never started the field is `null`.
                format: date-time
                type: string
              state:
                description: |-
                  ResourceState Current phase of the resource:
                  - pending: not available, waiting for other resources
                  - creating: not available, creation started
                  - active: available for data layer usage
                  - updating: available for data layer usage
                  - deleting: maybe still available for data layer user, can fail any moment
                  - error: failed to fulfill the request; would be related to provider issue or customer related input.
                type: string
            required:
            - conditions
            - powerState
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: skus.network.v1.secapi.cloud
spec:
  group: network.v1.secapi.cloud
  names:
    kind: SKU
    listKind: SKUList
    plural: skus
    shortNames:
    - sku
    singular: sku
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SKU is the API for getting network SKUs information.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSkuSpec Specification of the network SKU, including
              its bandwidth and packets per second.
            properties:
              bandwidth:
                description: Bandwidth The bandwidth in Mbps (Megabits per second).
                type: integer
              packets:
                description: Packets The number of packets per second (PPS) that the
                  network SKU can handle.
                type: integer
            required:
            - bandwidth
            - packets
            type: object
        type: object
    served: true
    storage: true

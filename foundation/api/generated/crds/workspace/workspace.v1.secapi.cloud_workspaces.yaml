---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: workspaces.workspace.v1.secapi.cloud
spec:
  group: workspace.v1.secapi.cloud
  names:
    kind: Workspace
    listKind: WorkspaceList
    plural: workspaces
    shortNames:
    - workspace
    singular: workspace
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Workspace is the API for getting the workspaces of a service.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          regionalCommonData:
            description: RegionalCommonData defines the additional common fields that
              can be set on regional resources
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Annotations User-defined key/value pairs that are mutable and can be used to add annotations.
                  The number of annotations is eventually limited by the CSP.
                type: object
              extensions:
                additionalProperties:
                  type: string
                description: |-
                  Extensions User-defined key/value pairs that are mutable and can be used to add extensions.
                  Extensions are subject to validation by the CSP, and any value that is not accepted will be rejected during admission.
                type: object
              labels:
                description: |-
                  Labels User-defined key/value pairs that are mutable and can be used to
                  organize and categorize resources. We store the keys explicitly in the spec, because the values will be stored
                  directly in the Kubernetes labels.
                items:
                  type: string
                type: array
            type: object
          spec:
            additionalProperties:
              type: string
            description: WorkspaceSpec Specification of the workspace, including its
              capabilities and extensions.
            type: object
          tenantRegionalCommonSpec:
            description: TenantRegionalCommonSpec defines the common spec fields for
              regional resources scoped to a tenant.
            properties:
              name:
                description: Name is the user-defined name of the resource.
                type: string
              region:
                description: Region is the identifier for the region where this resource
                  is located.
                type: string
              tenant:
                description: Tenant is the identifier for the tenant that owns this
                  resource.
                type: string
            required:
            - name
            - region
            - tenant
            type: object
        type: object
    served: true
    storage: true

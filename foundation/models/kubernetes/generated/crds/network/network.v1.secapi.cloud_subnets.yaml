---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: subnets.network.v1.secapi.cloud
spec:
  group: network.v1.secapi.cloud
  names:
    kind: Subnet
    listKind: SubnetList
    plural: subnets
    shortNames:
    - subnet
    singular: subnet
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Subnet is the API for managing network subnets.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          commonData:
            description: CommonData defines the additional common fields that can
              be set on resources
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Annotations User-defined key/value pairs that are mutable and can be used to add annotations.
                  The number of annotations is eventually limited by the CSP.
                type: object
              extensions:
                additionalProperties:
                  type: string
                description: |-
                  Extensions User-defined key/value pairs that are mutable and can be used to add extensions.
                  Extensions are subject to validation by the CSP, and any value that is not accepted will be rejected during admission.
                type: object
              labels:
                description: |-
                  Labels User-defined key/value pairs that are mutable and can be used to
                  organize and categorize resources. We store the keys explicitly in the spec, because the values will be stored
                  directly in the Kubernetes labels.
                items:
                  type: string
                type: array
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              SubnetSpec Detailed specification of the subnet. Automatic address
              assignment is supported, similar to the network configuration. The
              subnet's prefix length must be smaller than the network's prefix
              length, ensuring proper subdivision of the address space.
              The first and last IP addresses in the subnet are reserved
              the network address and broadcast address, respectively.

              Most CSP will not allow to use a different IPv6 prefix length than /64.
            properties:
              cidr:
                description: |-
                  Cidr Combined IPv4 and IPv6 CIDR block for a subnet. Depending on the network
                  configuration, either the IPv4 or IPv6 range can be omitted. So the following
                  combinations are possible:

                  * IPv4 only
                  * IPv6 only
                  * IPv4 and IPv6 (Dual Stack)
                properties:
                  ipv4:
                    description: Ipv4 IPv4 CIDR block for the subnet.
                    maxLength: 18
                    minLength: 9
                    type: string
                    x-kubernetes-validations:
                    - message: cidr.ipv4 must be a valid IPv4 CIDR block
                      rule: self.size() == 0 || (isCIDR(self) && cidr(self).ip().family()
                        == 4)
                  ipv6:
                    description: Ipv6 IPv6 CIDR block for the subnet.
                    maxLength: 43
                    minLength: 4
                    type: string
                    x-kubernetes-validations:
                    - message: cidr.ipv6 must be a valid IPv6 CIDR block
                      rule: self.size() == 0 || (isCIDR(self) && cidr(self).ip().family()
                        == 6)
                type: object
              routeTableRef:
                description: |-
                  RouteTableRef Reference to the route table used by default for all NICs in this Subnet.
                  If not provided, the routeTableRef associated with the network of the subnet will be used.
                properties:
                  provider:
                    description: Provider Provider of the resource. If not set, the
                      provider is inferred from the context.
                    maxLength: 64
                    type: string
                  region:
                    description: Region Region of the resource. If not set, the region
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  resource:
                    description: |-
                      Resource Resource-specific path identifying the resource within its workspace context.
                      This is the segment of the full URN after the provider, version, tenant, and
                      workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                      (nested) resources it includes the parent path segments:
                        `networks/<network-name>/route-tables/<rt-name>`
                      The provider, tenant, and workspace can be specified as separate fields in this
                      object; they do not need to be repeated here.
                    maxLength: 256
                    minLength: 1
                    type: string
                  tenant:
                    description: Tenant Tenant of the resource. If not set, the tenant
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  workspace:
                    description: Workspace Workspace of the resource. If not set,
                      the workspace is inferred from the context.
                    maxLength: 64
                    type: string
                required:
                - resource
                type: object
              skuRef:
                description: |-
                  SkuRef Reference to the SKU used by default for all NICs in this Network.
                  Can be overridden by the NIC
                properties:
                  provider:
                    description: Provider Provider of the resource. If not set, the
                      provider is inferred from the context.
                    maxLength: 64
                    type: string
                  region:
                    description: Region Region of the resource. If not set, the region
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  resource:
                    description: |-
                      Resource Resource-specific path identifying the resource within its workspace context.
                      This is the segment of the full URN after the provider, version, tenant, and
                      workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                      (nested) resources it includes the parent path segments:
                        `networks/<network-name>/route-tables/<rt-name>`
                      The provider, tenant, and workspace can be specified as separate fields in this
                      object; they do not need to be repeated here.
                    maxLength: 256
                    minLength: 1
                    type: string
                  tenant:
                    description: Tenant Tenant of the resource. If not set, the tenant
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  workspace:
                    description: Workspace Workspace of the resource. If not set,
                      the workspace is inferred from the context.
                    maxLength: 64
                    type: string
                required:
                - resource
                type: object
              zone:
                description: Zone Reference to a specific zone within a region
                maxLength: 32
                minLength: 1
                type: string
            required:
            - cidr
            - zone
            type: object
          status:
            description: SubnetStatus defines model for SubnetStatus.
            properties:
              cidr:
                description: |-
                  Cidr Combined IPv4 and IPv6 CIDR block for a subnet. Depending on the network
                  configuration, either the IPv4 or IPv6 range can be omitted. So the following
                  combinations are possible:

                  * IPv4 only
                  * IPv6 only
                  * IPv4 and IPv6 (Dual Stack)
                properties:
                  ipv4:
                    description: Ipv4 IPv4 CIDR block for the subnet.
                    maxLength: 18
                    minLength: 9
                    type: string
                    x-kubernetes-validations:
                    - message: cidr.ipv4 must be a valid IPv4 CIDR block
                      rule: self.size() == 0 || (isCIDR(self) && cidr(self).ip().family()
                        == 4)
                  ipv6:
                    description: Ipv6 IPv6 CIDR block for the subnet.
                    maxLength: 43
                    minLength: 4
                    type: string
                    x-kubernetes-validations:
                    - message: cidr.ipv6 must be a valid IPv6 CIDR block
                      rule: self.size() == 0 || (isCIDR(self) && cidr(self).ip().family()
                        == 6)
                type: object
              conditions:
                items:
                  description: |-
                    StatusCondition StatusCondition describes the state of a resource at a certain point.
                    Conditions are provider-specific and can represent different states depending on the
                    resource type and provider implementation.
                  properties:
                    lastTransitionAt:
                      description: |-
                        LastTransitionAt LastTransitionAt is the last time the condition transitioned from one
                        status to another. This should be when the underlying condition changed.
                        If that is not known, then using the time when the API field changed is
                        acceptable.
                      format: date-time
                      type: string
                    message:
                      description: Message A human-readable message indicating details
                        about the transition.
                      maxLength: 32768
                      type: string
                    occurrences:
                      type: integer
                    reason:
                      description: |-
                        Reason The reason for the condition's last transition in CamelCase.
                        The specific set of reason values is provider-specific and should be
                        documented by the provider.
                      maxLength: 1024
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    state:
                      description: |-
                        State Current phase of the resource:
                        - pending: not available, waiting for other resources
                        - creating: not available, creation started
                        - active: available for data layer usage
                        - updating: available for data layer usage
                        - deleting: maybe still available for data layer user, can fail any moment
                        - error: failed to fulfill the request; would be related to provider issue or customer related input.
                      type: string
                    type:
                      description: |-
                        Type Type of condition. The condition type is provider-specific and should
                        reflect the specific states relevant to your resource.
                      type: string
                  required:
                  - lastTransitionAt
                  - occurrences
                  - state
                  type: object
                maxItems: 32
                type: array
              routeTableRef:
                description: RouteTableRef The route table used by this subnet.
                properties:
                  provider:
                    description: Provider Provider of the resource. If not set, the
                      provider is inferred from the context.
                    maxLength: 64
                    type: string
                  region:
                    description: Region Region of the resource. If not set, the region
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  resource:
                    description: |-
                      Resource Resource-specific path identifying the resource within its workspace context.
                      This is the segment of the full URN after the provider, version, tenant, and
                      workspace parts. For a flat resource it is `<type>/<name>`, and for hierarchical
                      (nested) resources it includes the parent path segments:
                        `networks/<network-name>/route-tables/<rt-name>`
                      The provider, tenant, and workspace can be specified as separate fields in this
                      object; they do not need to be repeated here.
                    maxLength: 256
                    minLength: 1
                    type: string
                  tenant:
                    description: Tenant Tenant of the resource. If not set, the tenant
                      is inferred from the context.
                    maxLength: 64
                    type: string
                  workspace:
                    description: Workspace Workspace of the resource. If not set,
                      the workspace is inferred from the context.
                    maxLength: 64
                    type: string
                required:
                - resource
                type: object
              state:
                description: |-
                  ResourceState Current phase of the resource:
                  - pending: not available, waiting for other resources
                  - creating: not available, creation started
                  - active: available for data layer usage
                  - updating: available for data layer usage
                  - deleting: maybe still available for data layer user, can fail any moment
                  - error: failed to fulfill the request; would be related to provider issue or customer related input.
                type: string
            required:
            - conditions
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

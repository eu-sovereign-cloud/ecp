apiVersion: v1
kind: Service
metadata:
  name: gateway-regional-svc
  namespace: e2e-ecp
spec:
  type: ClusterIP
  selector:
    app: gateway-regional
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080

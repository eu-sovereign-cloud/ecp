# conformance.sh patches imagePullSecrets onto this SA at deploy time, but only
# when the local context carries registry credentials — KIND side-loads the
# runner image and needs none, so nothing is committed here.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: conformance-sa
